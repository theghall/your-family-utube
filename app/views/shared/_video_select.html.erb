<% if parent_mode? %>
  <div id="video_list_wrapper" class="<%= review_mode? ? 'col-md-5' : 'col-md-9' %>">
<% else %>
  <div id="video_list_wrapper" class="col-md-5">
<% end %>
  <%= render 'shared/tag_search' %>
  <div id="video_list">
	  <p>Click on a thumbnail to load it into the the player</p>
    <%= render 'profiles/video_list' unless @videos.nil? %>
  </div>
</div>
